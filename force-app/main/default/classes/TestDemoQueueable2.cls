public class TestDemoQueueable2 implements Queueable {
    public void execute (Queueablecontext axcz){
       list<contact> conlist = new list<contact>();
        contact con = new contact();
        con.LastName = ' DnyNID';
        conlist.add(con);
        insert conlist ;
     
    }
    
    
}